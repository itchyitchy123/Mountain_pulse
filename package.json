{
  "name": "mountainpulse",
  "version": "1.0.0",
  "private": true,
  "description": "Real-time mountain intelligence for skiers",
  "engines": {
    "node": ">=24 <25"
  },
  "packageManager": "npm@11.6.0",
  "scripts": {
    "start": "node server.js",
    "ci": "npm run check && npm test",
    "check": "node --check mountain-data.js && node --check data-platform.js && node --check adapters/scenario-adapter.js && node --check scoring.js && node --check route-engine.js && node --check safety-engine.js && node --check parking-model.js && node --check app.js && node --check server.js && node --check service-worker.js",
    "test": "node test.js"
  }
}
